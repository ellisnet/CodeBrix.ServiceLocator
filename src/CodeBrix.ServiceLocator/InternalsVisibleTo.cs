using System;
using System.Runtime.CompilerServices;

[assembly: InternalsVisibleTo("CodeBrix.ServiceLocator.Tests")]

// Preserved from the upstream CommonServiceLocator assembly attributes: the
// public surface is annotated [CLSCompliant(true)], so the assembly declares
// CLS compliance to match (and to satisfy CS3014).
[assembly: CLSCompliant(true)]
